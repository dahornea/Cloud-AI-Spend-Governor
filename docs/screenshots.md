# Screenshots To Capture

No screenshots are committed yet. Do not add fabricated screenshots. Capture real screenshots after running the local demo.

Save images under:

```txt
docs/assets/
```

Recommended captures:

- `dashboard-latest-scans.png`: dashboard with seeded scan history.
- `scan-cheap-pass.png`: cheap cloud change detail with PASS decision.
- `scan-expensive-cloud-fail.png`: expensive cloud change detail with FAIL decision and cost breakdown.
- `scan-ai-workflow.png`: expensive AI workflow detail with token estimate and assumptions.
- `pricing-metadata.png`: pricing metadata/confidence section.
- `project-budgets.png`: workspace/project budget configuration.
- `github-pr-report.png`: simulated or real GitHub PR report.
- `health-check.png`: `/health` returning healthy.

Before publishing screenshots, check that they do not reveal:

- Real tokens or webhook secrets.
- Private keys.
- Personal emails.
- Private repository names.
- Production database connection strings.
